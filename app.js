const greet = name => `Hello ${name}`;

module.exports = greet;


/*
 For configuring jasmine in front end, we should replace the test in the package.json
 "test": "jasmine-browser-runner runSpecs"
*/